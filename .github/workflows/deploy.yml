name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
<<<<<<< HEAD
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false
=======
>>>>>>> parent of 00eefea (refactor: separate build and deploy jobs in GitHub Pages workflow)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v4
<<<<<<< HEAD

=======
      
>>>>>>> parent of 00eefea (refactor: separate build and deploy jobs in GitHub Pages workflow)
    - name: Setup Node.js 📦
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
<<<<<<< HEAD

    - name: Install dependencies 🔧
      run: npm ci

    - name: Build 🏗️
      run: npm run build

    - name: Setup Pages
      uses: actions/configure-pages@v5

=======
        
    - name: Install dependencies 🔧
      run: npm ci
      
    - name: Build 🏗️
      run: npm run build
      
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
>>>>>>> parent of 00eefea (refactor: separate build and deploy jobs in GitHub Pages workflow)
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './dist'
<<<<<<< HEAD

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
=======
        
>>>>>>> parent of 00eefea (refactor: separate build and deploy jobs in GitHub Pages workflow)
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4